function [ sum ] = winsum( window )
%WINSUM Summary of this function goes here
%   Detailed explanation goes here
    shape = size(window);
    if length(shape) > 2
        error('Incorrect window');
    end
    winsize = shape(1);
    vecsize = 

end

